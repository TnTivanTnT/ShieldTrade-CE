version: '3.8'
services:
  shieldtrade-hybrid:
    build: .
    container_name: shieldtrade_v6_hybrid
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    environment:
      - TZ=Europe/Madrid